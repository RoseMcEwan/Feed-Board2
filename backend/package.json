{
  "name": "backend",
  "version": "1.0.0",
  "description": "Existing FeedBoard development fixtures and optional weather proxy. Not the final SQL backend.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
